<Window x:Class="BeltTensionTest.WPF.Views.DebugLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Debug Log" Width="600" Height="450"
        Background="#12121E" Foreground="#A0A0BE"
        FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="LogListBox"
                 Grid.Row="0"
                 Background="#0E0E1A"
                 Foreground="#A0A0BE"
                 FontFamily="Consolas"
                 FontSize="11"
                 BorderBrush="#46466A"
                 Margin="0,0,0,6"
                 SelectionMode="Extended"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       x:Name="StatusLabel"
                       VerticalAlignment="Center"
                       Text="Monitoring serial port..."/>
            <Button Grid.Column="1"
                    Content="Clear"
                    Width="70"
                    Margin="6,0"
                    Click="Clear_Click" Style="{DynamicResource BlueButton}"/>
            <Button Grid.Column="2"
                    Content="Copy All"
                    Width="80"
                    Click="CopyAll_Click" Style="{DynamicResource BlueButton}"/>
        </Grid>
    </Grid>
</Window>
