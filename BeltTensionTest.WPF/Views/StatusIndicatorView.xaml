<UserControl x:Class="BeltTensionTest.WPF.Views.StatusIndicatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             Height="24">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="{Binding Label, ElementName=Root}"
                   FontSize="{Binding FontSize, ElementName=Root}"
                   FontWeight="{Binding FontWeight, ElementName=Root}"
                   Foreground="#A0A0BE"
                   VerticalAlignment="Center"
                   Margin="0,0,6,0"/>
        <Ellipse Width="10" Height="10"
                 VerticalAlignment="Center">
            <Ellipse.Style>
                <Style TargetType="Ellipse">
                    <Setter Property="Fill" Value="#CC2222"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=Root}" Value="True">
                            <Setter Property="Fill" Value="#32CD32"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsEnabled, ElementName=Root}" Value="False">
                            <Setter Property="Fill" Value="#505068"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Ellipse.Style>
        </Ellipse>
    </StackPanel>
</UserControl>
