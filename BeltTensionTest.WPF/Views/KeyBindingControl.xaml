<UserControl x:Class="BeltTensionTest.WPF.Views.KeyBindingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="28">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="PART_Text" Grid.Column="0" Margin="6,4" IsReadOnly="True" Focusable="False" IsTabStop="False"
                 Background="{StaticResource BgLightBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="4,2"/>
        <CheckBox x:Name="PART_Global" Grid.Column="1" Content="Global" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{StaticResource TextPrimaryBrush}"/>
    </Grid>
</UserControl>